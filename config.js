// Deployment environments may replace this file with an API override.
// An empty value keeps the environment-aware fallback in getApiBaseUrl().
window.SMBWEB_API_BASE_URL = window.SMBWEB_API_BASE_URL || "";
